name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pdfplumber python-docx pyyaml colorama

      - name: Check syntax
        run: |
          python -m py_compile main.py
          python -m py_compile scanner/extractor.py
          python -m py_compile scanner/analyzer.py
          python -m py_compile scanner/metadata.py
          python -m py_compile scanner/reporter.py
          python -m py_compile scanner/sanitizer.py
          echo "All files compile successfully"